import React, {Component} from 'react'
// import Playlists from '../components/Playlists'

debugger;
//const getVisibleTodos = (todos, filter) => {
//const getVisiblePlaylists = (playlists) =>

export default class VisiblePlaylists extends Component {
  constructor(props) {
    super(props);

    this.state = {
      playlists: [
          {
              id: 1,
              name: "ppp1"
          },
          {
              id: 2,
              name: "ppp2"
          }
      ]
    };
  }

  render() {
     return (
       <div>
         <h1>Visible</h1>
       </div>
     );
  }
}
